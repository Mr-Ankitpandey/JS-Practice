sendmail function:-
make a new form object
extract all data from form using Object.entries()
check for base condition (initial valuues of from and to) and alert if no values are selected
create a new object which has id, from, to, subject, message, isRead
now get allmails array form getmail function and then push this object into the array and set it into localstorage with jsonstringify
reset subject and message inputs only



